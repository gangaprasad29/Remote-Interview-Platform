module.exports = {
  // This ensures that the output directory is set to 'frontend/dist'
  // and that the build is properly optimized for production
  output: 'export',
  distDir: 'frontend/dist',
  images: {
    unoptimized: true
  }
}
